type TestRun @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    testName: String! @indexed
    timestamp: String!
    durationSecs: Float
    clients: Int
    results: String
    summary: String
    snapshots: String
}

# ── REST Read tables ──
type ReadBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}
type ReadAuthor @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    name: String!
}

# ── REST Write tables ──
type WriteBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}
type WriteAuthor @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    name: String!
}

# ── REST Update tables ──
type UpdateBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}
type UpdateAuthor @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    name: String!
}

# ── REST Join tables ──
type JoinBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
    author: JoinAuthor @relationship(from: "authorId")
}
type JoinAuthor @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    name: String!
}

# ── GraphQL Read tables ──
type GqlReadBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}

# ── GraphQL Write tables ──
type GqlWriteBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}

# ── GraphQL Update tables ──
type GqlUpdateBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}

# ── GraphQL Join tables ──
type GqlJoinBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
    author: GqlJoinAuthor @relationship(from: "authorId")
}
type GqlJoinAuthor @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    name: String!
}

# ── REST Batch Write tables ──
type BatchWriteBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}

# ── GraphQL Batch Write tables ──
type GqlBatchWriteBook @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, update, delete]) {
    id: ID!
    title: String!
    price: Float
    authorId: ID!
}

# ── Vector tables ──
type EmbedArticle @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, delete]) {
    id: ID!
    title: String!
    author: String!
    category: String! @indexed
    content: String!
    embedding: Vector @indexed(source: "content")
}

type SearchArticle @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, delete]) {
    id: ID!
    title: String!
    author: String!
    category: String! @indexed
    content: String!
    embedding: Vector @indexed(source: "content")
}

# ── Blob table ──
type BlobData @table(database: "app-benchmarks") @export(mqtt: false) @access(public: [read, create, delete]) {
    id: ID!
    title: String!
    author: String!
    category: String!
    content: String!
}

# ── Realtime tables ──
type WsMessage @table(database: "app-benchmarks") @export(rest: true, sse: true, ws: true, mqtt: false) @access(public: [read, create, delete, subscribe, connect]) {
    id: ID!
    title: String!
    content: String!
}

type SseMessage @table(database: "app-benchmarks") @export(rest: true, sse: true, ws: true, mqtt: false) @access(public: [read, create, delete, subscribe, connect]) {
    id: ID!
    title: String!
    content: String!
}

# ── WebSocket Fan-In (publish) tables ──
type WsPublishMessage @table(database: "app-benchmarks") @export(rest: true, sse: true, ws: true, mqtt: false) @access(public: [read, create, delete, subscribe, connect, publish]) {
    id: ID!
    title: String!
    content: String!
}

# ── MQTT tables ──
type MqttMessage @table(database: "app-benchmarks") @export(rest: true, sse: false, ws: false, mqtt: true) @access(public: [read, create, delete, subscribe, connect, publish]) {
    id: ID!
    title: String!
    content: String!
}
